package com.mdove.passwordguard.greendao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.mdove.passwordguard.greendao.entity.SucSelfTask;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "SUC_SELF_TASK".
*/
public class SucSelfTaskDao extends AbstractDao<SucSelfTask, Long> {

    public static final String TABLENAME = "SUC_SELF_TASK";

    /**
     * Properties of entity SucSelfTask.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property MTask = new Property(1, String.class, "mTask", false, "M_TASK");
        public final static Property MTime = new Property(2, long.class, "mTime", false, "M_TIME");
        public final static Property MIsSuc = new Property(3, int.class, "mIsSuc", false, "M_IS_SUC");
        public final static Property MIsSee = new Property(4, int.class, "mIsSee", false, "M_IS_SEE");
        public final static Property MPriority = new Property(5, int.class, "mPriority", false, "M_PRIORITY");
        public final static Property MBelongId = new Property(6, long.class, "mBelongId", false, "M_BELONG_ID");
    }


    public SucSelfTaskDao(DaoConfig config) {
        super(config);
    }
    
    public SucSelfTaskDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"SUC_SELF_TASK\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"M_TASK\" TEXT," + // 1: mTask
                "\"M_TIME\" INTEGER NOT NULL ," + // 2: mTime
                "\"M_IS_SUC\" INTEGER NOT NULL ," + // 3: mIsSuc
                "\"M_IS_SEE\" INTEGER NOT NULL ," + // 4: mIsSee
                "\"M_PRIORITY\" INTEGER NOT NULL ," + // 5: mPriority
                "\"M_BELONG_ID\" INTEGER NOT NULL );"); // 6: mBelongId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"SUC_SELF_TASK\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, SucSelfTask entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String mTask = entity.getMTask();
        if (mTask != null) {
            stmt.bindString(2, mTask);
        }
        stmt.bindLong(3, entity.getMTime());
        stmt.bindLong(4, entity.getMIsSuc());
        stmt.bindLong(5, entity.getMIsSee());
        stmt.bindLong(6, entity.getMPriority());
        stmt.bindLong(7, entity.getMBelongId());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, SucSelfTask entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String mTask = entity.getMTask();
        if (mTask != null) {
            stmt.bindString(2, mTask);
        }
        stmt.bindLong(3, entity.getMTime());
        stmt.bindLong(4, entity.getMIsSuc());
        stmt.bindLong(5, entity.getMIsSee());
        stmt.bindLong(6, entity.getMPriority());
        stmt.bindLong(7, entity.getMBelongId());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public SucSelfTask readEntity(Cursor cursor, int offset) {
        SucSelfTask entity = new SucSelfTask( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // mTask
            cursor.getLong(offset + 2), // mTime
            cursor.getInt(offset + 3), // mIsSuc
            cursor.getInt(offset + 4), // mIsSee
            cursor.getInt(offset + 5), // mPriority
            cursor.getLong(offset + 6) // mBelongId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, SucSelfTask entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setMTask(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setMTime(cursor.getLong(offset + 2));
        entity.setMIsSuc(cursor.getInt(offset + 3));
        entity.setMIsSee(cursor.getInt(offset + 4));
        entity.setMPriority(cursor.getInt(offset + 5));
        entity.setMBelongId(cursor.getLong(offset + 6));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(SucSelfTask entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(SucSelfTask entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(SucSelfTask entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
